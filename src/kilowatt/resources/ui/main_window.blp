using Gtk 4.0;
using Adw 1;

translation-domain "io.github.dlippok.kilowatt";

Adw.ApplicationWindow main_window {
    title: _("Application Template");

    Box {
        orientation: vertical;

        HeaderBar { }

        Label {
            label: _("Application Template");
            margin-top: 24;
            margin-bottom: 24;
            vexpand: true;
        }

        $ExampleButton { }
    }
}
